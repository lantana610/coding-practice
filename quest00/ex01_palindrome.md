# Exercise 1: The Palindrome Challenge
## Write pseudocode for a function that check if a string is a palindrome 

write a functoin that checks if a string is a palindrome or not
    write the functoin 
       reverse (the string [from start: to stop: minus 1]) 
    if the string is palindrome
        print "it is a palindrome"
    else
    print "it is not a palindrome"
    END IF CONDITION   
    END FUNCTION
## implement your solution in python 
 ```python

 def palindrome(str): # defining the function
    reverse = (newchar[::-1]) # slice of reverse of the string
    if newchar == reverse: #  comparing the reverse to the initial string
        return str, "is a palindrome" # returning if it a palindrome
    else: 
        return str, "is not a  palindrome" # returning if it not a palindrome

```
## What did you learn from solving it before asking AI?  
       I learnt how to define a functoin in python, and how to make a slice in python. 

 ## How is your understanding different now? 
      it improving,i have made improvment on my way of understanding code and how to implement them  

  ## Could you now write similar functions (e.g., reverse a string) without help? 
       yes 





